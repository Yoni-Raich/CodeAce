# from CodeAce.mapping_agent import mappingAgent
# from CodeAce.core_agent import coreAgent as agent

# src = ""

# mapp_agent = mappingAgent("llm name", "src_path", "AppData_path")
# mapp_agent.run_mapping_process()

# agent = coreAgent(src, AppData = null)
# 1. agent.set_prompt("prompt")
# 2. list_rl_file = agent.search_relevet_files()
# 3. res = agent.invoke(rl_file)